import React from "react";
import { SearchWrap } from "./style";

const BackTestInput = (props) => {
  return (
    <SearchWrap>
      <input type="text" />
      <button>검색</button>
    </SearchWrap>
  );
};

export default BackTestInput;
